<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - GAMINGCORE</title>
    <style>
        /* RESET & BASE STYLES */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }
        
        body {
            background-color: #0f0f1a;
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* NAVBAR (MATCHING OTHER PAGES) */
        nav {
            background: #1a1a2e;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .nav-brand {
            font-size: 1.5rem;
            font-weight: bold;
            color: white;
        }
        
        .nav-links {
            display: flex;
            gap: 1.5rem;
        }
        
        .nav-links a {
            color: #e6e6e6;
            text-decoration: none;
        }
        
        .signup-btn {
            background: #4cc9f0;
            color: #0f0f1a;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            font-weight: bold;
            text-decoration: none;
        }

        /* 404 CONTENT */
        .error-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
        }

        .error-code {
            font-size: 5rem;
            color: #4cc9f0;
            margin-bottom: 1rem;
        }

        .error-message {
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            max-width: 600px;
            line-height: 1.5;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            margin: 2rem 0;
        }

        .btn {
            padding: 0.8rem 1.5rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s;
        }

        .btn-primary {
            background: #4cc9f0;
            color: #0f0f1a;
        }

        .btn-secondary {
            background: transparent;
            color: #4cc9f0;
            border: 2px solid #4cc9f0;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        /* FOOTER (CENTERED) */
        footer {
            text-align: center;
            padding: 1.5rem;
            background: #1a1a2e;
        }
    </style>
</head>
<body>
    <!-- NAVBAR (CONSISTENT WITH OTHER PAGES) -->
    <nav>
        <div class="nav-brand">GAMINGCORE</div>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/leaderboard">Leaderboard</a>
            <a href="/login">Login</a>
            <a href="/register" class="signup-btn">Sign Up Free</a>
        </div>
    </nav>

    <!-- 404 CONTENT -->
    <div class="error-container">
        <h1 class="error-code">404</h1>
        <h2 class="error-message">Level Not Found</h2>
        <p>The page you're looking for has either been deleted, moved, or never existed.<br>
        Maybe you took a wrong turn in the dungeon! Let's get you back to safety.</p>
        
        <div class="action-buttons">
            <a href="/" class="btn btn-primary">Return Home</a>
            <a href="/leaderboard" class="btn btn-secondary">View Leaderboard</a>
        </div>
    </div>

    <!-- FOOTER -->
    <footer>
        &copy; 2025 Game Leaderboard
    </footer>
</body>
</html>